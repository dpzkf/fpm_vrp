import { Form } from "./Form";
import * as Styled from "./styles.ts";

export const Discount = () => {
  return (
    <Styled.Wrapper w="100%" gap={40} direction="column">
      <Form />
    </Styled.Wrapper>
  );
};
